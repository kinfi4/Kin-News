version: "3"

services:
  kin-frontend:
    build:
      context: .
      dockerfile: ./etc/Dockerfile
    volumes:
      - ./kin-frontend:/app
    ports:
      - "3000:3000"
    env_file:
      - ./etc/env/.dev.env
    command: npm start
